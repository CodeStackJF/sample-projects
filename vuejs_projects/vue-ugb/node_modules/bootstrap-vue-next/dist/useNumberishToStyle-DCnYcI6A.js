import { f as RX_NUMBER } from "./constants-Cap5sNf8.js";
import { computed, toValue } from "vue";
//#region src/composables/useNumberishToStyle.ts
var useNumberishToStyle = (el, unit = "px") => computed(() => {
	const value = toValue(el);
	const resolvedUnit = toValue(unit);
	return RX_NUMBER.test(String(value)) ? `${Number(value)}${resolvedUnit}` : value;
});
//#endregion
export { useNumberishToStyle as t };

//# sourceMappingURL=useNumberishToStyle-DCnYcI6A.js.map