import { i as getSafeDocument } from "../../../dom-BdUQrX99.js";
import { i as omit } from "../../../object-C7YmCGEm.js";
import { useScrollspy } from "../../composables/useScrollspy/index.mjs";
import { r as getDirectiveUid } from "../../../utils-nRGGHA_i.js";
import "vue";
//#region src/directives/BScrollspy/index.ts
var bind = (el, binding) => {
	if (getSafeDocument() === null) return;
	const uid = getDirectiveUid(binding);
	const elWithScrollspy = el;
	elWithScrollspy.$__scrollspy = elWithScrollspy.$__scrollspy ?? Object.create(null);
	const existingInstance = elWithScrollspy.$__scrollspy[uid];
	if (existingInstance) existingInstance.cleanup();
	const { arg, value } = binding;
	const isObject = typeof value === "object" && value !== null;
	const content = arg ? arg : typeof value === "string" ? value : isObject ? value.content || value.element : null;
	elWithScrollspy.$__scrollspy[uid] = useScrollspy(content, el, isObject ? omit(value, ["content", "element"]) : {});
};
var vBScrollspy = {
	mounted: bind,
	updated: bind,
	beforeUnmount(el, binding) {
		const uid = getDirectiveUid(binding);
		const elWithScrollspy = el;
		const instance = elWithScrollspy.$__scrollspy?.[uid];
		if (!instance) return;
		instance.cleanup();
		delete elWithScrollspy.$__scrollspy[uid];
	}
};
//#endregion
export { vBScrollspy };

//# sourceMappingURL=index.mjs.map