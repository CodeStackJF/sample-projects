import { c as useColorMode$1 } from "../../../dist-9-XTMhb-.js";
//#region src/composables/useColorMode/index.ts
var useColorMode = (opts = {}) => {
	const persist = opts.persist ?? false;
	const attribute = "data-bs-theme";
	const selector = "html";
	return useColorMode$1({
		attribute,
		selector,
		storageKey: persist ? `bv-color-${opts.attribute ?? attribute}-${opts.selector ?? selector}` : null,
		...opts
	});
};
//#endregion
export { useColorMode };

//# sourceMappingURL=index.mjs.map